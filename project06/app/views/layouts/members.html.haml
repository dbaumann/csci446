!!!
%html{html_attrs}

  %head
    %title
      = h(yield(:title) || "Untitled")
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag 'reset', 'application'
    = yield(:head)

  %body
    #container
      #masthead
        %h1
          =link_to "Gamez", members_root_url
        #user_nav
          = link_to "Edit Profile", edit_members_user_path(:current)
          %br
          = link_to "Logout", logout_path
      #menubar
        %ul#menubar
          %li= link_to "Games", members_games_path, :title => "View games"
          %li= link_to "Users", members_users_path, :title => "View users"
      #content
        - flash.each do |name, msg|
          = content_tag :div, msg, :id => "flash_#{name}"

        - if show_title?
          %h2=h yield(:title)

        = yield
